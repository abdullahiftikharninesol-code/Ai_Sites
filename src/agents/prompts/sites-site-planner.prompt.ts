/**
 * The invariant SITE_PLANNING system instruction extracted from the existing
 * intelligence request helper. Runtime request content remains a user message.
 */
export const SITES_SITE_PLANNER_PROMPT = `SITES_AGENT_TASK:SITE_PLANNING
Return only the requested compact JSON. Never include secrets, credentials, hidden reasoning, source code, or unsupported capabilities.`;
